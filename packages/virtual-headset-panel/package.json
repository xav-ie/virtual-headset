{
  "name": "virtual-headset-panel",
  "private": true,
  "description": "AGS panel for virtual-headset mute + source selection (the app itself is built with ags via Nix; this is just for unit-testing logic.ts)",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
